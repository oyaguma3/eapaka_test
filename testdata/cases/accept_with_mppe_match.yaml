version: 1
name: accept_with_mppe_match
identity: "0440100123456789@wlan.mnc010.mcc440.3gppnetwork.org"
expect:
  result: accept
  mppe:
    require_present: true
    # 固定テストデータ運用時にのみ指定する（MPPE暗号化済み生値）。
    # RFC 9048 KDF 対応後は、Access-Accept の MPPE 属性生値を再取得して貼り付ける。
    send_key: "hex:"
    recv_key: "hex:"
